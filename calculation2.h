#ifndef CALCULATION2_H
#define CALCULATION2_H

#include <stdio.h>

void secondCalculation(int a, int b)
{
    int result = a * b;

    printf("The result of the second calculation (multiplication) is: %d\n", result);
}

#endif