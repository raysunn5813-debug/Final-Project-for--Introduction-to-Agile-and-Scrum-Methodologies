#ifndef CALCULATION1_H
#define CALCULATION1_H

#include <stdio.h>

void firstCalculation(int a, int b)
{
    int result = a + b;

    printf("The result of the first calculation (addition) is: %d\n", result);
}

#endif